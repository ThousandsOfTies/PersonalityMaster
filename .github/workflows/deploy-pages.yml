name: GitHub Pages Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Pages deployment is disabled
        run: echo "GitHub Pages deployment has been intentionally disabled."
